#include <stdio.h>#include <string.h>char command[256] = "";int main(argc,argv)int argc; char *argv[];{  int res;  FILE *task;  char *prog, *type;  type = argv[1];  prog = argv[2];  strcpy(command, prog);  strcat(command, " ");  strcat(command, argv[3]);  printf("opening %s\n", prog);  task = popen(command, type);  fprintf(task, "def\n");     fprintf(task, "abc\n");  fprintf(task, "xyz\n");  pclose(task);  printf("Exiting!\n");  }